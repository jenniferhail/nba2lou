var $ = require('jquery');

module.exports = {
	init: function () {
		// Commenting out vue since it is not used on this site.
		// var app = new Vue({
		// 	el: '#app',
		// 	data: {
		// 		layouts: document.querySelectorAll('.layout')
		// 	}
		// })
	}
}